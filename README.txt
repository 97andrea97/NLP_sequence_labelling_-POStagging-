The task adressed is POS tagging.

The notebook "NLP_sequence_labelling(POStagging)" contains everything: the code do define models deploying LST, GRU, CRF simply specifying the flags; 

It contains also the code to download the dataset and train the model.
